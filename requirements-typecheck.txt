# 类型检查依赖（CI mypy job 专用）
# 目的：让 `mypy core/` 看到与本地一致的第三方类型（尤其 sqlmodel/SQLAlchemy table=True 类定义），
# 而不是把缺失模块当 Any（缺失时 mypy 会对 `class X(SQLModel, table=True)` 报 call-arg）。
# 说明：
# - 仅含 core/ 顶层实际 import 的类型相关依赖；torch/transformers/accelerate 等 ML 重量依赖不装
#   （对应导入均在 try/惰性守卫内，--ignore-missing-imports 下按 Any 处理，等价于无 torch 降级路径）。
# - 约束与 requirements.txt / pyproject 保持一致（此处不额外收紧）。
pydantic>=2.0
pydantic-ai>=0.3.0
sqlmodel>=0.0.22
sqlalchemy>=2.0
networkx>=3.0
numpy>=1.24
httpx>=0.27
requests>=2.28
logfire>=3.0
structlog>=24.1
python-json-logger>=2.0
PyYAML>=6.0
jieba>=0.42
ebooklib>=0.18
